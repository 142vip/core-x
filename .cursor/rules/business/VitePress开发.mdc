---
description: VitePress 文档站与 @142vip/vitepress 开发规范
globs: "{packages/vitepress/**,apps/vitepress-demo/**,.vitepress/**,docs/**}"
alwaysApply: false
---

# VitePress 开发

## 文档站与代码同步（必守）

**代码变更 → 同步文档与站点**（细则 `engineering/09-文档同步规范`）。交付前必须做 **09 文档自检**。

### 三层须一致

| 层 | 路径 | 说明 |
|----|------|------|
| 主题包 | `packages/vitepress/` | `@142vip/vitepress` 源码、组件、API |
| 根站配置 | `.vitepress/` | 导航、侧栏、首页、主题扩展 |
| 站点正文 | `packages/*/README.md`、`apps/*/README.md`、`docs/`（见 `09` 文档地图） |

### 改什么 → 同步什么

| 改什么 | 同步哪里 |
|--------|----------|
| 新增/删除包或 demo | `sidebar.ts`、根 README 包列表、`16`、对应 `README.md` |
| 包 API / 用法 | `packages/*/README.md`；刷新 `docs/apis/`（`build:docs`） |
| demo 端口/命令 | `apps/*/README.md`、`08` |
| 导航 / 版本 / rewrites | `.vitepress/config.ts` |
| 侧栏分类 / 链接 | `.vitepress/sidebar.ts` |
| 首页表格 / demo 列表 | `sidebar.ts`、`project-data.ts`（`DEMO_APP_DIRS` 若 demo 名变） |
| 首页 UI | `theme/components/HomePage.vue`、`docs/index.md`（**仅** hero + HomePage） |
| 主题继承 / 注册 | `theme/index.ts` 对齐 `@142vip/vitepress` |

**禁止**：文档有、代码无（幽灵条目）；代码有、sidebar/README 无（遗漏）；手改 `docs/apis/` 生成文件。

### `.vitepress/` 文件职责

```
.vitepress/
├── config.ts                 # nav、rewrites、主题配置、版本
├── sidebar.ts                # 侧栏（30 包 + 4 demo）
├── theme/
│   ├── index.ts              # defineVipExtendsTheme
│   ├── style.css
│   └── components/
│       ├── HomePage.vue
│       └── project-data.ts   # 首页数据（读 sidebar + package.json）
docs/index.md                 # 首页 frontmatter + HomePage
```

## 核心 API（不可破坏）

```ts
defineVipVitepressConfig(userConfig)                    // 原样返回
defineVipVitepressConfig(userConfig, { mermaid: true }) // 增量合并 markdown + vite
```

## 文件职责（packages/vitepress）

| 文件 | 职责 |
|------|------|
| `core/vitepress.ts` | 配置入口 |
| `core/mermaid.ts` | fence 插件 + Vite 预构建 |
| `core/sass-vite.ts` | Sass modern API（Mermaid 启用时合并） |
| `theme/index.ts` | `defineVipExtendsTheme` |
| `components/VipMermaid.vue` | 渲染组件 |

## 依赖边界

- `OPEN_SOURCE_ADDRESS` → 直接从 `@142vip/open-source` 导入
- `vipDocSite` / `VipPackageJSON` → `@142vip/utils`
- 复制用 `copy-to-clipboard`，勿再封装

## 版本锁定

- vitepress `1.6.4`（当前稳定版最新）
- element-plus `2.13.7`（2.13 线补丁；勿升 2.14，见 `engineering/03-代码质量`）

## 验证

```bash
cd packages/vitepress && pnpm build && pnpm typecheck
cd apps/vitepress-demo && pnpm build
pnpm build:docs              # 根站 + typedoc:api；验证 .vitepress 与文档一致
pnpm dev                     # 可选预览 :8080
```
