---
description: 按用户意图定位目标包、路径与验证命令
alwaysApply: true
---

# 任务路由索引

> 收到任务后，用本表定位「改哪里、读什么规则、跑什么命令」。

## 意图 → 包 → 路径 → 验证

| 意图 | 目标包 | 关键路径 | 验证命令 |
|------|--------|----------|----------|
| 文档站主题/组件 | `@142vip/vitepress` | `packages/vitepress/src/` | `build` + `typecheck` + `vitepress-demo build` |
| 根文档配置 | — | `.vitepress/config.ts` | `pnpm build:docs` 或 `pnpm dev` |
| demo 文档 | `vitepress-demo` | `apps/vitepress-demo/.vitepress/` | `pnpm --filter vitepress-demo build` |
| Mermaid 图表 | `@142vip/vitepress` | `components/VipMermaid.vue`, `core/mermaid.ts` | 同上 |
| Nest 启动/配置 | `@142vip/nest-starter` | `packages/nest-starter/src/` | `test` + `nest-demo` |
| Nest 日志/Redis/ORM | `nest-logger` 等 | `packages/nest-*/` | `pnpm --filter @142vip/nest-* test` |
| Egg 插件 | `@142vip/egg-*` | `packages/egg-*/src/` | `test` + `egg-demo test` |
| HTTP 客户端 | `@142vip/axios` | `packages/axios/src/` | `test` + `build` |
| gRPC | `@142vip/grpc` | `packages/grpc/src/` | `test` + `build` |
| 数据源 | `@142vip/data-source` | `packages/data-source/src/` | `test` + `build` |
| CLI 工具 | `@142vip/fairy-cli` | `packages/fairy-cli/src/` | `test` + `build` |
| 版本发布 | `@142vip/release-version` | `packages/release-version/` | `test` |
| ESLint 规则 | `@142vip/eslint-config` | `packages/eslint-config/` | 根 `pnpm lint:fix` |
| 开源链接/常量 | `@142vip/open-source` | `packages/open-source/src/constants/` | `build` |
| VuePress | `@142vip/vuepress` | `packages/vuepress/` | `vuepress-demo build` |

## 跨包改动判断

```
只改一个包？     → 单包 build/test 即可
改公开 API？     → 检查 dependents + demo + 文档
改 peerDeps？    → 检查所有消费方 package.json
改根 ESLint/TS？ → 全仓 lint:fix + 关键包 build
```

## Demo 对照表

| Demo | 验证命令 | 覆盖包 |
|------|----------|--------|
| `vitepress-demo` | `pnpm --filter vitepress-demo build` | vitepress, open-source, utils |
| `nest-demo` | `pnpm --filter nest-demo start` | nest-starter 等 |
| `egg-demo` | `pnpm --filter egg-demo test` | egg 插件族 |
| `vuepress-demo` | `pnpm --filter vuepress-demo build` | vuepress |

## 不确定时

1. `Grep` 关键词定位引用
2. 读 `包职责地图` 确认边界
3. 问用户而非猜测包归属

## 命令写法

`pnpm --filter <name> <script>` 与 `cd` 到对应 `packages/` / `apps/` 目录后执行 `pnpm <script>` **等价**（详见 `engineering/08-常用命令手册`）。根级脚本（`pnpm dev`、`build:docs`、`lint:fix`）仅在仓库根执行。
