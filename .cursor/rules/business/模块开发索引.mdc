---
description: 【业务】技术栈专项规则索引（globs 自动加载）
alwaysApply: true
---

# 模块开发索引

> 包职责与验证命令见 **`任务路由索引`**；文档同步见 **`engineering/09`**。

## globs → 规则

| 规则 | 触发路径 |
|------|----------|
| `TypeScript规范` | `**/*.{ts,tsx}` |
| `Vue组件规范` | `**/*.vue` |
| `unbuild打包` | `**/build.config.ts` |
| `Nest开发` | `packages/nest*/**` |
| `Egg开发` | `packages/egg*/**` |
| `VitePress开发` | `vitepress`、`.vitepress`、`vitepress-demo`、`docs/**` |
| `测试规范` | `**/test/**` |

## alwaysApply（business/）

`包职责地图` · `任务路由索引` · `代码风格统一` · `ESM与模块发布`

实现时同时遵循 `engineering/01` 与上表匹配项。
