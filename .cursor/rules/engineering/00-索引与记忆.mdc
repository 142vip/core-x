---
description: 【必读】规则索引与仓库记忆
alwaysApply: true
---

# 索引与记忆

> 模型单次任务路径：`codegraph sync` → **`16`** → **`business/任务路由`** → 读代码小改 → **`01`** 流程

## 速查（31 包 · 4 demo）

| 项 | 值 |
|----|-----|
| 安装 | `./scripts/ci` 首选；失败 → `pnpm install` |
| build | `cd packages/<dir>` 或 `cd apps/<demo>` 后 `pnpm build` |
| 结束 | `lint:fix`（0 error）→ 牵连 build → **`09` 文档自检** |
| 文档 | 根 README + `docs/` + `.vitepress/` + 包 README，与代码同步 |
| 图表 | 根 README ↔ `07` 五段同构；变更见 `10` 同步铁律 |
| 对外 | **不引用** `.cursor/rules`（`14`） |

## 读哪条规则

| 第一步 | 文件 |
|--------|------|
| 不知道改哪 | `business/任务路由索引` |
| 不知道有哪些包 | `engineering/16-全仓资产清单` |
| 执行顺序 | `engineering/01` → `02` 验收 |
| 命令怎么跑 | `engineering/08` |
| 文档怎么同步 | `engineering/09`（含 `docs/`） |
| 写代码风格 | `business/代码风格统一` + 路径匹配专项（`business/模块开发索引`） |

## 目录

```
engineering/   00~17  流程·命令·文档·Mermaid·scripts
business/      包职责·路由·风格·ESM·技术栈专项
```
