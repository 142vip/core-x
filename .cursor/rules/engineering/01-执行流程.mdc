---
description: 【必读】大模型开发工作流（分步执行，严格按序）
alwaysApply: true
---

# 执行流程

> 配合 `13-模型协作规范` 分步执行、简洁交付。图表规范见 `10-Mermaid图表规范`。

## 总览

```mermaid
flowchart TD
  S0["0 索引 codegraph sync + 16"] --> S1["1 定位 读代码·小范围"]
  S1 --> S2["2 调研 列文件清单"]
  S2 --> S3["3 实现 模块风格"]
  S3 --> S5["5 自检 局部 lint"]
  S5 --> S4["4 构建 牵连模块局部 build"]
  S4 --> S6["6 验收 02+09 文档自检"]
  S6 --> S7["7 收尾 06"]
  S7 --> S7b["沉淀 12 更新 rules"]
```

## 0 · 索引

```bash
codegraph sync    # 无索引 → codegraph init
```

读 `00-索引`、`16-全仓资产清单`、`TODO.md`、business/模块规则。

## 1 · 定位

- `任务路由索引` → 目标包
- **Read/Grep 最新代码**，小范围改动
- 不明确则问用户

## 2 · 调研（只读）

- 同模块 3~5 文件，对齐 `business/代码风格统一`
- 输出：待改文件清单 + 每步 verify 命令

## 3 · 实现

- 编码 + 同步文档（`09`）+ **若影响架构/流程则更新 README·07 Mermaid**（`10`）
- 新知识沉淀 rules（`12`）

## 4 · 构建验证（**在 lint 之后 · 局部优先**）

先完成 §5 局部 lint，再对**直接牵连**的包 / demo build。**禁止**无必要时跑全仓 `pnpm build`。

### 确定范围（够用即停）

| 改动位置 | 至少 build |
|----------|------------|
| `packages/<pkg>/` | `cd packages/<pkg> && pnpm build` |
| `apps/<demo>/` | `cd apps/<demo> && pnpm build` |
| `@142vip/vitepress` / `.vitepress/` | `packages/vuepress` + `apps/vuepress-demo` |
| 改 `exports` / `peerDeps` | 该包 + **直接** workspace 引用方 |
| 仅 `.cursor/rules/**`、纯问答、仅 `.md` | **跳过** |
| 跨多包 / 根构建链 / 仍不明 | **最后保底** `pnpm build` |

```bash
cd packages/<pkg> && pnpm build
cd packages/<pkg> && pnpm typecheck   # 有脚本且涉及
cd packages/<pkg> && pnpm test        # 逻辑变更时
```

- [ ] 牵连模块 `build` **exit 0**（未牵连的**不跑**）
- [ ] **禁止**为保险默认全仓 build

## 5 · 自检 lint（必做 · 在构建之前 · **局部优先**）

```bash
# 默认
npx eslint packages/<pkg> --fix
npx eslint apps/<demo> --fix

# 改根 ESLint/TS、跨多顶层目录、用户将 commit
pnpm lint:fix
```

| 可跳过 | 须局部 lint | 须全仓 `pnpm lint:fix` |
|--------|-------------|------------------------|
| 纯问答、仅 rules | 单包 / demo / 指定文件 | 根 `eslint.config.js`、根 TS 配置 |

- **零 ESLint error**（warning 允许）
- **禁止**无必要时全仓 lint

## 6 · 验收（含文档自检）

`02-验收校验` 全部 relevant 项通过，**必须完成 §E 文档与 VitePress 自检**（`09` 对照表）。

## 7 · 收尾

`06-收尾与环境清理`：端口、临时文件、空目录。

## 禁止

跳步、未 lint 交付、**lint 后有 error**、**牵连模块 build 失败**、**无必要跑全仓 lint/build**、**代码改了文档未同步**、大面积未读代码就改、冗长无效输出（见 `13`）
