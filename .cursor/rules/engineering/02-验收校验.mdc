---
description: 【必读】功能完成后的验收与校验清单
alwaysApply: true
---

# 验收校验

**全部 relevant 项通过方可交付。** 配合 `01-执行流程` 使用。顺序：**局部 lint → 局部 build → 文档自检**。

> **强约束**：自检**默认局部**；与本次改动无关的包 / 目录**跳过**；全仓 `pnpm lint:fix` / `pnpm build` **仅作最后保底**。

## A. 通用验收（每次必做）

- [ ] 已执行 `codegraph sync`（或 `init`）
- [ ] 先学最新代码，改动范围小且可追溯
- [ ] 遵循 `business/代码风格统一`、`03-代码质量`、`13-模型协作规范`（简洁输出）
- [ ] 中文回复用户

## B. ESLint 与编辑器（**局部 lint 优先**）

```bash
npx eslint packages/<pkg> --fix
npx eslint apps/<demo> --fix

# 改根 ESLint/TS、跨多顶层目录、用户将 commit
pnpm lint:fix
```

| 可跳过 | 须局部 lint | 须全仓 `pnpm lint:fix` |
|--------|-------------|------------------------|
| 纯问答、仅 `.cursor/rules/**` | 单包 / demo / 指定文件 | 根 `eslint.config.js`、根 TS 配置 |
| 仅 `.md` 无 TS/JS | | 跨 `packages/` + `apps/` + 根配置 |

- [ ] 改动路径 **零 ESLint error**（warning 允许）
- [ ] **不得因本次改动新增 error**
- [ ] **禁止**无必要时全仓 lint
- [ ] 不修改 `eslint.config.js` 的 `ignores` 掩盖 error
- [ ] 已全局忽略：`docs/apis/**`、`docs/wiki/**`

## C. 构建验证（**lint 之后 · 局部 build 优先**）

仅对**直接牵连**的包 / demo build；**未改动的不跑**。

```bash
cd packages/<pkg> && pnpm build
cd apps/<demo> && pnpm build
cd packages/<pkg> && pnpm typecheck    # 有脚本且涉及

# 仅范围仍不明时
pnpm build
```

| 场景 | 最低 build | 可跳过 |
|------|------------|--------|
| 改单包 | `packages/<pkg>` | 未牵连 demo 可不跑 demo |
| 改 demo | `apps/<demo>` + 其 workspace 依赖（若需要） | — |
| 改 vitepress | `packages/vuepress` + `apps/vuepress-demo` | 小改不加 `build:docs` |
| 改 exports / peerDeps | 该包 + **直接**引用方 | — |
| 仅 rules / 纯问答 / 仅 `.md` | — | **跳过** |
| 仍不明 | `pnpm build` | 最后手段 |

- [ ] 牵连模块 `build` **exit 0**
- [ ] **禁止**为保险默认全仓 build
- [ ] 新增导出已同步 `package.json` exports / 入口 index

## D. 测试

有逻辑变更时，**仅跑牵连包**：

```bash
cd packages/<pkg> && pnpm test
```

- [ ] 相关 `test/*.spec.ts` 通过
- [ ] bug 修复优先补 repro 测试

## E. 文档与站点自检（代码变更后必做 · 见 `09`）

完成 lint + build 后，**逐项对照 `09` 自检表**，确认文档与代码匹配。

- [ ] 根 README / 包 README / demo README 与本次代码改动一致
- [ ] **清单完整**：30 包 + 4 demo，无遗漏、无幽灵条目、无死链
- [ ] `.vitepress/sidebar.ts` 与 `packages/`、`apps/` 一一对应
- [ ] **`.vitepress/`** 已检查：`config.ts`（nav/版本）、`theme/` 首页数据与代码一致
- [ ] **`docs/`** 与代码一致：`index.md` 仅首页；`tech.md`/`media.md` 手改内容已更新；`apis/` 已生成
- [ ] API 变更已 `build:docs` 或 `typedoc:api`
- [ ] 根 README 与 `07` **五段 Mermaid 同构**（`10` 变更对照表）；Markdown 符合 `17`
- [ ] `TODO.md` 仅含未完成项；rules 外无 `.cursor/rules` 引用（`14`）

## F. 按域专项

### VitePress

```bash
cd packages/vuepress && pnpm build && pnpm typecheck
cd apps/vuepress-demo && pnpm build
```

- [ ] `defineVipVitepressConfig(config)` 单参原样返回
- [ ] element-plus 保持 2.13.7（勿升 2.14）
- [ ] 文档站、`.vitepress/` 与代码结构一致（`09` 自检表已全部勾选）

### Nest / Egg / unbuild

见 `business/` 对应文件。

## G. Git（仅用户要求时）

- [ ] commit 格式、分支策略正确
- [ ] 无敏感文件

## H. 收尾与环境

见 `06-收尾与环境清理`：

- [ ] 端口已释放、无临时文件/空目录
- [ ] `git status` 无应删垃圾

## 失败处理

```
失败项 → 修复 → 重跑局部 lint → 重跑局部 build → 再勾选
```

不得带 **lint error** 或 **build 失败** 交付。**禁止**无必要时用全仓命令代替局部验证。
