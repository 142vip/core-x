---
description: 【必读】功能完成后的验收与校验清单
alwaysApply: true
---

# 验收校验

**全部 relevant 项通过方可交付。** 配合 `01-执行流程` 使用。顺序：**lint:fix → 局部 build → 文档自检**。

## A. 通用验收（每次必做）

- [ ] 已执行 `codegraph sync`（或 `init`）
- [ ] 先学最新代码，改动范围小且可追溯
- [ ] 遵循 `business/代码风格统一`、`03-代码质量`、`13-模型协作规范`（简洁输出）
- [ ] 中文回复用户

## B. ESLint 与编辑器（每次必做 · lint:fix 后）

```bash
pnpm lint:fix
```

- [ ] **`pnpm lint` 零 error**（**warning 允许**，不要求清零历史 warning）
- [ ] **不得因本次改动新增 error**；尽量不新增 warning
- [ ] 改动文件在 IDE 中**无 TypeScript 错误**；ESLint 无 error 级诊断
- [ ] pre-commit 钩子可正常通过（钩子跑 `lint:fix`，须无 error）
- [ ] 不修改 `eslint.config.js` 的 `ignores` 来掩盖本次引入的 **error**
- [ ] 已全局忽略：`docs/apis/**`、`docs/wiki/**`（TypeDoc 生成物）

## C. 构建验证（**lint:fix 之后必做**）

对本次变更**牵连**的包 / demo 做局部 build；范围不确定时跑全量 `pnpm build`。

```bash
# 局部（首选：进入目录）
cd packages/<pkg> && pnpm build
cd apps/<demo> && pnpm build
cd packages/<pkg> && pnpm typecheck    # 若有

# 保底（根目录）
pnpm build
```

| 场景 | 最低 build 范围 |
|------|-----------------|
| 改单包 | `cd packages/<pkg> && pnpm build` + 消费方 demo |
| 改 demo | `cd apps/<demo> && pnpm build` + 其 `@142vip/*` 依赖 |
| 改 vitepress / 根文档 | `cd packages/vitepress && pnpm build` + `cd apps/vitepress-demo && pnpm build`；大改加 `pnpm build:docs` |
| 改 exports / peerDeps | 该包 + 所有 workspace 引用方 `build` |
| 不确定 | `pnpm build` |

- [ ] 牵连模块 / 应用 `build` **exit 0**
- [ ] `typecheck` exit 0（若包有该脚本且本次涉及）
- [ ] 新增导出已同步 `package.json` exports / 入口 index

## D. 测试

有逻辑变更时：

```bash
cd packages/<pkg> && pnpm test
```

- [ ] 相关 `test/*.spec.ts` 通过
- [ ] bug 修复优先补 repro 测试

## E. 文档与站点自检（代码变更后必做 · 见 `09`）

完成 lint + build 后，**逐项对照 `09` 自检表**，确认文档与代码匹配。

- [ ] 根 README / 包 README / demo README 与本次代码改动一致
- [ ] **清单完整**：30 包 + 4 demo，无遗漏、无幽灵条目、无死链
- [ ] `.vitepress/sidebar.ts` 与 `packages/`、`apps/` 一一对应
- [ ] **`.vitepress/`** 已检查：`config.ts`（nav/版本）、`theme/` 首页数据与代码一致
- [ ] **`docs/`** 与代码一致：`index.md` 仅首页；`tech.md`/`media.md` 手改内容已更新；`apis/` 已生成
- [ ] API 变更已 `build:docs` 或 `typedoc:api`
- [ ] 根 README 与 `07` **五段 Mermaid 同构**（`10` 变更对照表）；Markdown 符合 `17`
- [ ] `TODO.md` 仅含未完成项；rules 外无 `.cursor/rules` 引用（`14`）

## F. 按域专项

### VitePress

```bash
cd packages/vitepress && pnpm build && pnpm typecheck
cd apps/vitepress-demo && pnpm build
```

- [ ] `defineVipVitepressConfig(config)` 单参原样返回
- [ ] element-plus 保持 2.13.1
- [ ] 文档站、`.vitepress/` 与代码结构一致（`09` 自检表已全部勾选）

### Nest / Egg / unbuild

见 `business/` 对应文件。

## G. Git（仅用户要求时）

- [ ] commit 格式、分支策略正确
- [ ] 无敏感文件

## H. 收尾与环境

见 `06-收尾与环境清理`：

- [ ] 端口已释放、无临时文件/空目录
- [ ] `git status` 无应删垃圾

## 失败处理

```
失败项 → 修复 → 重跑 lint:fix → 重跑 build → 再勾选
```

不得带 **lint error** 或 **build 失败** 交付。
