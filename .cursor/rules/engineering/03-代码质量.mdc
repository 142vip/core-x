---
description: 代码规范、注释原则、最小改动纪律
alwaysApply: true
---

# 代码质量

## 核心原则

1. **正确优先**：逻辑清晰、类型准确、边界处理合理
2. **最小改动**：每行变更须能追溯到用户需求
3. **风格一致**：匹配周边文件的命名、结构、注释语言
4. **注释克制**：解释「为什么」，不描述「做了什么」

## 注释规范

### 要写

- 非显而易见的业务规则（如「单参须原样返回以保持兼容」）
- 技术陷阱规避（如 Nest DI 不能用 type import）
- 外部依赖限制（如 element-plus 版本锁定原因）
- 公开 API 的 JSDoc：`@param` `@returns` `@example`

### 不要写

```ts
// ❌ 重复代码语义
// 获取用户信息
const user = getUser()

// ❌ 变更日志式
// 2024-01-01 修改为 xxx

// ❌ 显而易见的步骤
// 遍历数组
items.forEach(...)
```

### 示例

```ts
// ✅ 说明非显而易见的行为
// 无第二参数时原样返回，避免破坏历史调用方
if (options == null) return userConfig

// ✅ 说明外部约束
// element-plus 2.14 触发 @vueuse/core@14 的 Rollup 告警，锁定 2.13.x（当前 2.13.7）
```

## 编码纪律

### 做

- 优先复用包内已有函数，不重复造轮子
- 导出类型与实现同步维护
- 空值判断用 `!= null` 同时覆盖 null/undefined
- 改动产生的无用 import/变量一并清理

### 不做

- 未请求的功能、配置项、抽象层
- 单处使用的 helper 函数
- 不可能触发的 error handling
- 顺手「优化」相邻无关代码
- 200 行能 50 行解决的过度设计
- **手改** `package.json` `version` 或 `CHANGELOG.md`（见 `05` §版本与 CHANGELOG）
- 无意义数组拷贝：`compactMap([...iterable], …)`、`[...alreadyArray]` 仅为了再传参——`matchAll` 用 `Array.from`，已是数组则直传
- 泛化变量名 `result` / `data` / `res` 承载业务语义——用具名变量（如 `articles`、`syncPayload`）
- 领域层重复 `String(x).trim()`：边界 `normalize*` / DTO 一次收窄后，内层信任类型

## `@142vip/utils` · `vipLodash` 扩展

lodash 二次封装统一挂在 **`vipLodash`** 上，**禁止**再从根入口单独 import 扩展方法：

| 扩展 | 用途 |
|------|------|
| `vipLodash.isJsonRecord` | 外部 JSON 平面对象守卫（`isPlainObject` + 类型收窄） |
| `vipLodash.toJsonRecord` | `unknown` → `JsonRecord`，非对象返回 `{}` |
| `vipLodash.compactMap` | `map` + `compact` 去 falsy，替代 `.map().filter()` |

```ts
import { vipLodash, type JsonRecord } from '@142vip/utils'

const row = vipLodash.toJsonRecord(raw)
if (vipLodash.isJsonRecord(nested)) { /* … */ }
const names = vipLodash.compactMap(users, u => u.nickname)
```

应用内自写 `isRecord` / 顶层 `compactMap` import：优先改为 `vipLodash.*`（JSON 边界语义用 `isJsonRecord`）。

**`vipLodash` 实现约束**：`{ ...lodashBase, ...extensions }` 展开挂载；扩展键**不得**与 lodash 原生同名（禁止覆盖 `map` / `pick` 等）。

## TypeScript

- 优先 `interface` 描述对象；工具类型用 `type`
- 避免 `any`；必要时局部使用并注释
- Nest：**禁止**对注入类使用 `import type`（见 `business/TypeScript规范`）

## 改动自检（实现中随时对照）

```
□ 是否只改了任务相关文件？
□ 公开 API 行为是否保持不变？
□ 注释是否都有存在价值？
□ 是否有可删除的冗余代码？
□ 是否已对齐目标模块风格（见 `business/代码风格统一`）？
```

## 任务结束（必须 · **局部优先**）

```bash
# 1. 局部 lint（默认）
npx eslint packages/<pkg> --fix

# 2. 局部 build（仅牵连包）
cd packages/<pkg> && pnpm build && pnpm typecheck   # 有 typecheck 时
```

| 步骤 | 默认 | 全仓保底（少用） |
|------|------|------------------|
| Lint | `npx eslint <改动路径> --fix` | `pnpm lint:fix` |
| Build | `cd packages/<pkg> && pnpm build` | `pnpm build` |

**可跳过 lint + build**：纯问答、仅 `.cursor/rules/**`、仅 `.md` 且未动 TS/JS。

- 改动路径 **零 ESLint error**；warning 允许，勿新增 error
- 牵连包 `build` exit 0
- **强约束**：禁止无必要时跑全仓 `pnpm lint:fix` / `pnpm build`

## 沟通

- 用户要求中文回复
- 代码引用格式：`startLine:endLine:filepath`
