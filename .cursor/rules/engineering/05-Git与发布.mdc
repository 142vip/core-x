---
description: Conventional Commits、分类提交、分支策略、PR、发布、发版 playbook、Agent 约束
alwaysApply: true
---

# Git 与发布

## Commit 格式

```
<type>(<scope>): <subject>
# 或（无 scope）
<type>: <subject>
```

| 字段 | 规则 |
|------|------|
| type | `feat` `fix` `hotfix` `docs` `style` `refactor` `perf` `test` `build` `ci` `chore` `revert` `release` |
| scope | 包名（如 `@142vip/utils`、`vitepress`）或 `CHANGELOG` `README` `release`；跨模块根级 chore/docs 可省略 |
| subject | 祈使、简洁；中文为主；5–100 字符 |

### type 选用（必守）

| 场景 | 推荐 type | 勿用 |
|------|-----------|------|
| 新功能、能力变更、对外行为变化 | `feat` | `chore` |
| 缺陷修复 | `fix` / `hotfix` | `chore` |
| 依赖升级且影响运行/构建产物 | `build` | `chore` |
| 演示站验证业务功能 | `feat` / `test`（视是否新增用例） | `chore` |
| **`.cursor/**` 提示词、规则、配置** | **`chore`** | `docs` / `feat` |
| 仓库内其他文档、规范、注释（非 `.cursor`） | `docs` | — |
| 纯工具链/脚本/配置，**不改变**业务行为 | `chore` | — |

**`chore` 仅用于与业务功能无关的维护**（如 release 元数据、无关脚本整理、**`.cursor` 目录**）。凡影响包 API、站点行为、依赖运行时、demo 功能验证的提交，不得使用 `chore`。

校验：`commit-msg` → `pnpm check:commit` → `scripts/core/verify-commit.ts`

### Commit 文案真实性（强制）

- **标题与 staged 文件一一对应**：`git diff --cached --name-only` 列出的每个路径，须能在标题或正文找到对应模块/对象；**禁止**标题写 A 包却 stage B 包文件。
- commit 标题与正文必须能在本次 diff 中逐条对照，禁止「看起来正确但无法落地到代码」的空泛描述。
- 禁止套话模板（如「优化体验」「提升稳定性」）单独成句，必须写明具体对象（包/模块/接口/配置键）。
- 一次提交含多类改动时，按 `§分类提交` 拆成多条 commit；每条 commit 只描述该批次实际改动。
- 优先写「为何改」与「关键变化点」，避免流水账（罗列无关文件、重复形容词）。
- 提交前自检：`git diff --cached --name-only` 与 commit 文案逐项核对，不一致则重写文案再提交。

### 分类提交（强制）

用户要求交付 / 整理 / 提交时，**禁止**把多类改动揉进单个 commit。

#### 分类维度（由大到小 · 禁止乱归类）

```
包 / 应用（packages/<name> · apps/<name>）
  → 功能域 / 模块（如 utils/lodash、nest、docs/apis）
    → 单次改动主题（如「合并类型守卫」「补充 README 示例」）
```

| 层级 | 要求 |
|------|------|
| **包 / 应用** | `scope` 与 staged 路径一致；**禁止**单条 commit 跨多个无关联 `packages/*` 或 package 与 app 混提（根级 `chore`/`docs` 除外） |
| **功能 / 模块** | 同一 commit 只含**一个**业务域或子目录；**禁止**无关包拼盘 |
| **主题** | 标题描述的就是本批全部文件的共同目的；删/建/改 import 跟随所属模块同批提交 |

建议顺序：类型 / 接口 → 实现 → 测试 → 文档 → `TODO` / rules。

#### 单条粒度

| 原则 | 要求 |
|------|------|
| **文件数** | 默认 **≤15** 个路径；未获用户明确放宽时超 15 **必须**再拆 |
| 一条一事 | 每条 commit 只含**同一模块 + 同一主题** |
| 文案对照 | staged 每一文件均须在标题/正文可追溯到 |
| 只 stage 本批 | 只 `git add` 本批路径；**禁止** `git add -A` |
| 禁止空泛 | 标题禁止仅写「整理代码」「优化结构」；须写明包 + 模块 + 对象 |

#### Push

- Agent **只 `git commit`，不执行 `git push`**；推送由用户手动操作（agent 提交后列出 commit 摘要供用户审阅、自行推送）。
- **push 由用户手动执行**；agent 不运行 `git push`（即使经确认也不代跑）。

### Subject / Body 反引号（Markdown 可读）

涉及**英文单词、短语、标识符、路径、变量、API、配置键**时，用 Markdown 反引号包裹，便于 GitHub / 文档渲染：

| 宜加 `` `...` `` | 示例 |
|---|---|
| 包名 / 应用名（正文中） | `` `@142vip/utils` `` · `` `fairy-cli` `` |
| 变量 / 常量 / 类型 | `` `vipLodash.compactMap` `` · `` `DateFormatTemplate` `` · `` `TTL` `` |
| 路径 / 路由 / 文件 | `` `/api/v1` `` · `` `.agents/skills` `` · `` `sidebar.ts` `` |
| 英文专名 / 协议词 | `` `OIDC` `` · `` `CDN` `` · `` `ESM` `` · `` `unbuild` `` |

中文产品简称（如「文档站」「软著」）一般不加反引号。

```
feat(@142vip/utils): `vipLodash` 挂载 `compactMap` 与 JSON 守卫
docs(@142vip/nest-starter): 补充 `BaseVo` 使用说明
fix(@142vip/vitepress): 修复 `mermaid` 全屏异常
```

### Commit trailer（协作署名 · 默认禁止）

Agent **默认不在** commit message 中写任何 `Co-authored-by` / `Made-with` 等协作署名 trailer（**禁止**大模型 / IDE / Agent 品牌痕迹）。

| 规则 | 要求 |
|---|---|
| 默认 | **只写** `type(scope): subject` 与必要正文；**不加** trailer |
| 用户明确要求署名 | 仅可追加**一行** `Co-authored-by: AI Fan <mmdapl@163.com>` |
| **禁止** | `Co-authored-by: Cursor <cursoragent@cursor.com>`、`Made-with: Cursor`、Claude/GPT/Copilot 等 AI 产品 trailer；正文中的「Generated by …」「via Agent」等声明 |
| 提交后自检 | `git log -1 --format=%B` 不得含上述禁止行；若环境自动注入，须用 `git commit-tree` 重写**未推送** commit 剔除后再继续 |

勿依赖环境自动注入的 Cursor 署名；禁止为「消 Cursor 行」而默认附带其它 AI trailer。

## 分支

| 变更 | 目标分支 |
|------|----------|
| 新特性 | `next` |
| bugfix / docs / 其他 | `main` |

npm 发布分支：`next`（`.npmrc`）

## 发布 Commit

| 场景 | message 前缀 | 触发（push 到 `next` 后） |
|------|--------------|--------------------------|
| GitHub Release（仓库根版本） | `chore(release): ...` | `CD.yaml` → `npx changelog` + Pages |
| npm 包发布 | `` release(@142vip/xxx): publish `vX.Y.Z` `` | `CD.yaml` → npm OIDC Trusted Publishing |

## 模块发版（Monorepo · Agent 必守）

用户明确要求「发布 / release」时才进入流程。目标分支 **`next`**。

### 0. 人工二次确认（必做 · 稳重）

发版**不可**「一口气跑完」。在真正 bump / commit **之前**，须向用户确认（**push 由用户手动**，agent 不执行）：

1. `pnpm check`（= `check:release` + `check:publish`）的 ✖ 清单（自上次 `release(scope)` 后有未发版提交的包）
2. 拟定发布顺序与预计下一版本（如 `…-alpha.N+1`）
3. 本轮提交后是否由用户手动 push（触发 CI / npm 发布）

用户确认「可以发 / 继续」后再执行 commit；**push 一律由用户手动操作**，agent 不执行。

**禁止**：跳过确认直接 `pnpm release` / bumpx / 批量 push；agent 不执行任何 push。

### 1. 检查谁要发

```bash
pnpm check            # = check:release + check:publish
# 或仅：pnpm check:release   （fa release --check-release）
```

| 标记 | 含义 |
|------|------|
| ✖ | 自上次 `release(scope)` 后有未发版提交 |
| ✔ | 已对齐，无需发 |

### 2. 顺序（铁律）

```
packages（有 ✖ 的）→ apps（有 ✖ 的）
```

一次**只处理一个**模块：升版本 → commit →（**由用户手动** push）→ 再下一个。
原因：CD 只看 **`github.event.head_commit.message`**；一次推多个 `release`，只有 HEAD 那条触发流水线。

本地可先只 commit、暂不 push；**push 由用户手动操作**（见 `§Agent 约束`）。

### 3. 推荐做法：`pnpm release`

```bash
pnpm release   # fa release --vip -F './apps/*' -F './packages/*' --check-branch next --check-branch main
```

交互：选模块 → 确认 → 选版本（预发默认 **next** = `prerelease` + `alpha`）→ 写 `package.json` + `CHANGELOG.md` → commit →（用户手动）push。

工具默认可能 push：**Agent 不执行 push**；会 push 的命令由用户手动运行，agent 只做本地 commit。

等价底层（`@142vip/release-version`）：

- commit：`` release(<pkg.name>): publish `v%s` ``
- `execute`：`git add CHANGELOG.md`
- 子模块通常 `tag: false`

### 4. Commit 文案与 CI 触发（铁律）

```
release(@142vip/utils): publish `v0.0.1-alpha.57`
chore(release): publish v0.0.1-alpha.42        # 仓库根版本，与子模块分开
```

- 本地常 **无** npm login（`npm whoami` 401 正常）；**npm 上传靠 CI Trusted Publishing**
- 本地**不**手动 `pnpm publish`（除非用户明确且已登录）
- 根版本发版（`chore(release):`）仅在用户要求发仓库主版本、且子模块已全 ✔、并经二次确认后进行

### 5. 踩坑（已验证）

| 坑 | 正确做法 |
|----|----------|
| 手写 commit message 时前缀错误 → CD **不触发** | 用 `pnpm release` 交互生成；或确保 `release(@…): publish \`v…\`` 逐字正确 |
| 错误 message 已 push | **禁止** force push；可补一条**同版本**空 commit，message 写成正确的 `release(...)`，**由用户手动** push |
| `npx changelog` 无 `GITHUB_TOKEN` 时 exit 1 | 文件通常已写好，确认 `CHANGELOG.md` 有新版本节即可继续 |
| 多模块一次 push | 拆开，每模块单独 push，且每步确认（**push 由用户手动执行**） |

### 6. 收尾

```bash
pnpm check:release   # 期望全 ✔
```

## 版本与 CHANGELOG（Agent · 必守）

`pnpm release` / `fa release` 会**自动** bump 各包 `package.json` 的 `version` 并写入 `CHANGELOG.md`。

| 禁止（除非用户明确要求手工发版） | 应做 |
|------|------|
| 手改 `packages/*/package.json` 的 `version` | 发版走 `pnpm release`，或等维护者交互发版 |
| 手改 `packages/*/CHANGELOG.md` | 由 release 工具生成；文档只改 `README` 等对外说明 |
| 为「对齐版本」提前改 CHANGELOG 占位 | 在 `README` / rules 记待发能力；发版后再对齐 lock |

日常功能迭代：**只改 `src` + `README`**；版本号与 CHANGELOG 留给 release 流程。

## PR 自查（`.github/PULL_REQUEST_TEMPLATE/pr_cn.md`）

- 需求背景、方案、是否 breaking
- 文档 / 演示 / TS / Changelog 勾选
- 新特性 → `next`，需维护者审核

## Hooks

| Hook | 行为 |
|------|------|
| `pre-commit` | `pnpm lint:fix` |
| `commit-msg` | `pnpm check:commit` → `scripts/core/verify-commit.ts` |

## Agent 约束

| 操作 | 条件 |
|------|------|
| **commit** | 用户明确要求提交时可执行（写清 message；只 stage 相关文件） |
| **push / 推远程** | **agent 不执行**；提交后列出 commit 摘要，由用户手动 `git push` |
| **PR** | 用户明确要求才创建 |

其它：

- 用户未要求 commit 时：**不**主动提交
- hook 失败：修复后**新 commit**，不 amend 已推送
- 禁止：改 `git config`、`--no-verify`；不对 main force push（回滚等由用户手动处理）
- 分批提交：只 `git add` 本批文件；勿改 hook 配置绕过校验
- **禁止**在 commit message 中写入 AI / Agent / IDE 相关 trailer 或署名（见上文）
