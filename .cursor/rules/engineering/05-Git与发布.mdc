---
description: Conventional Commits、分类提交、分支策略、PR、发布、Agent 约束
alwaysApply: true
---

# Git 与发布

## Commit 格式

```
<type>(<scope>): <subject>
# 或（无 scope）
<type>: <subject>
```

| 字段 | 规则 |
|------|------|
| type | `feat` `fix` `hotfix` `docs` `style` `refactor` `perf` `test` `build` `ci` `chore` `revert` `release` |
| scope | 包名（如 `@142vip/utils`、`vitepress`）或 `CHANGELOG` `README` `release`；跨模块根级 chore/docs 可省略 |
| subject | 祈使、简洁；中文为主；5–100 字符 |

### type 选用（必守）

| 场景 | 推荐 type | 勿用 |
|------|-----------|------|
| 新功能、能力变更、对外行为变化 | `feat` | `chore` |
| 缺陷修复 | `fix` / `hotfix` | `chore` |
| 依赖升级且影响运行/构建产物 | `build` | `chore` |
| 演示站验证业务功能 | `feat` / `test`（视是否新增用例） | `chore` |
| **`.cursor/**` 提示词、规则、配置** | **`chore`** | `docs` / `feat` |
| 仓库内其他文档、规范、注释（非 `.cursor`） | `docs` | — |
| 纯工具链/脚本/配置，**不改变**业务行为 | `chore` | — |

**`chore` 仅用于与业务功能无关的维护**（如 release 元数据、无关脚本整理、**`.cursor` 目录**）。凡影响包 API、站点行为、依赖运行时、demo 功能验证的提交，不得使用 `chore`。

校验：`commit-msg` → `pnpm check:commit` → `scripts/core/verify-commit.ts`

### Commit 文案真实性（强制）

- **标题与 staged 文件一一对应**：`git diff --cached --name-only` 列出的每个路径，须能在标题或正文找到对应模块/对象；**禁止**标题写 A 包却 stage B 包文件。
- commit 标题与正文必须能在本次 diff 中逐条对照，禁止「看起来正确但无法落地到代码」的空泛描述。
- 禁止套话模板（如「优化体验」「提升稳定性」）单独成句，必须写明具体对象（包/模块/接口/配置键）。
- 一次提交含多类改动时，按 `§分类提交` 拆成多条 commit；每条 commit 只描述该批次实际改动。
- 优先写「为何改」与「关键变化点」，避免流水账（罗列无关文件、重复形容词）。
- 提交前自检：`git diff --cached --name-only` 与 commit 文案逐项核对，不一致则重写文案再提交。

### 分类提交（强制）

用户要求交付 / 整理 / 提交时，**禁止**把多类改动揉进单个 commit。

#### 分类维度（由大到小 · 禁止乱归类）

```
包 / 应用（packages/<name> · apps/<name>）
  → 功能域 / 模块（如 utils/lodash、nest、docs/apis）
    → 单次改动主题（如「合并类型守卫」「补充 README 示例」）
```

| 层级 | 要求 |
|------|------|
| **包 / 应用** | `scope` 与 staged 路径一致；**禁止**单条 commit 跨多个无关联 `packages/*` 或 package 与 app 混提（根级 `chore`/`docs` 除外） |
| **功能 / 模块** | 同一 commit 只含**一个**业务域或子目录；**禁止**无关包拼盘 |
| **主题** | 标题描述的就是本批全部文件的共同目的；删/建/改 import 跟随所属模块同批提交 |

建议顺序：类型 / 接口 → 实现 → 测试 → 文档 → `TODO` / rules。

#### 单条粒度

| 原则 | 要求 |
|------|------|
| **文件数** | 默认 **≤15** 个路径；未获用户明确放宽时超 15 **必须**再拆 |
| 一条一事 | 每条 commit 只含**同一模块 + 同一主题** |
| 文案对照 | staged 每一文件均须在标题/正文可追溯到 |
| 只 stage 本批 | 只 `git add` 本批路径；**禁止** `git add -A` |
| 禁止空泛 | 标题禁止仅写「整理代码」「优化结构」；须写明包 + 模块 + 对象 |

#### Push

- Agent **只 `git commit`，默认不 `git push`**；远程由用户审阅 commit 后**自行推送**。
- 用户明确要求 push 时，须先列出将推送的 commit 摘要，**二次确认**后再执行。

### Subject / Body 反引号（Markdown 可读）

涉及**英文单词、短语、标识符、路径、变量、API、配置键**时，用反引号包裹：

```
feat(@142vip/utils): `vipLodash` 挂载 `compactMap` 与 JSON 守卫
docs(nest): 补充 `BaseVo` 使用说明
```

### Commit trailer（协作署名 · 默认禁止）

Agent **默认不在** commit message 中写任何 `Co-authored-by` / `Made-with` 等协作署名 trailer（**禁止**大模型 / IDE / Agent 品牌痕迹）。

| 规则 | 要求 |
|---|---|
| 默认 | **只写** `type(scope): subject` 与必要正文；**不加** trailer |
| 用户明确要求署名 | 仅可追加**一行** `Co-authored-by: AI Fan <mmdapl@163.com>` |
| **禁止** | `Co-authored-by: Cursor <cursoragent@cursor.com>`、`Made-with: Cursor`、Claude/GPT/Copilot 等 AI 产品 trailer；正文中的「Generated by …」「via Agent」等声明 |
| 提交后自检 | `git log -1 --format=%B` 不得含上述禁止行；若环境自动注入，须用 `git commit-tree` 重写**未推送** commit 剔除后再继续 |

勿依赖环境自动注入的 Cursor 署名；禁止为「消 Cursor 行」而默认附带其它 AI trailer。

## 分支

| 变更 | 目标分支 |
|------|----------|
| 新特性 | `next` |
| bugfix / docs / 其他 | `main` |

npm 发布分支：`next`（`.npmrc`）

## 发布 Commit

- GitHub Release：`chore(release): ...`
- npm：`release(@142vip/xxx): publish \`vX.Y.Z\``

## 版本与 CHANGELOG（Agent · 必守）

`pnpm release` / `fa release` 会**自动** bump 各包 `package.json` 的 `version` 并写入 `CHANGELOG.md`。

| 禁止（除非用户明确要求手工发版） | 应做 |
|------|------|
| 手改 `packages/*/package.json` 的 `version` | 发版走 `pnpm release`，或等维护者交互发版 |
| 手改 `packages/*/CHANGELOG.md` | 由 release 工具生成；文档只改 `README` 等对外说明 |
| 为「对齐版本」提前改 CHANGELOG 占位 | 在 `README` / rules 记待发能力；发版后再对齐 lock |

日常功能迭代：**只改 `src` + `README`**；版本号与 CHANGELOG 留给 release 流程。

## PR 自查（`.github/PULL_REQUEST_TEMPLATE/pr_cn.md`）

- 需求背景、方案、是否 breaking
- 文档 / 演示 / TS / Changelog 勾选
- 新特性 → `next`，需维护者审核

## Hooks

| Hook | 行为 |
|------|------|
| `pre-commit` | `pnpm lint:fix` |
| `commit-msg` | `pnpm check:commit` → `scripts/core/verify-commit.ts` |

## Agent 约束

| 操作 | 条件 |
|------|------|
| **commit** | 用户明确要求提交时可执行（写清 message；只 stage 相关文件） |
| **push / 推远程** | **禁止擅自执行**；须先列出将推送的 commit 摘要，**征得用户二次确认**后再 `git push` |
| **PR** | 用户明确要求才创建 |

其它：

- 用户未要求 commit 时：**不**主动提交
- hook 失败：修复后**新 commit**，不 amend 已推送
- 禁止：改 `git config`、`--no-verify`、对 main force push（除非明确要求）
- 分批提交：只 `git add` 本批文件；勿改 hook 配置绕过校验
- **禁止**在 commit message 中写入 AI / Agent / IDE 相关 trailer 或署名（见上文）
