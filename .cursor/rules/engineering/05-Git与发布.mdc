---
description: Conventional Commits、分支策略、PR、发布
alwaysApply: true
---

# Git 与发布

## Commit 格式

```
<type>(<scope>): <subject>
```

| 字段 | 规则 |
|------|------|
| type | `feat` `fix` `hotfix` `docs` `style` `refactor` `perf` `test` `build` `ci` `chore` `revert` `release` |
| scope | 包名（如 `vitepress`）或 `CHANGELOG` `README` `release` |
| subject | 5–100 字符 |

### type 选用（必守）

| 场景 | 推荐 type | 勿用 |
|------|-----------|------|
| 新功能、能力变更、对外行为变化 | `feat` | `chore` |
| 缺陷修复 | `fix` / `hotfix` | `chore` |
| 依赖升级且影响运行/构建产物 | `build` | `chore` |
| 演示站验证业务功能 | `feat` / `test`（视是否新增用例） | `chore` |
| **`.cursor/**` 提示词、规则、配置** | **`chore`** | `docs` / `feat` |
| 仓库内其他文档、规范、注释（非 `.cursor`） | `docs` | — |
| 纯工具链/脚本/配置，**不改变**业务行为 | `chore` | — |

**`chore` 仅用于与业务功能无关的维护**（如 release 元数据、无关脚本整理、**`.cursor` 目录**）。凡影响包 API、站点行为、依赖运行时、demo 功能验证的提交，不得使用 `chore`。

**`.cursor` 提交示例**（scope 可省略）：

```
chore: 补充 Git 提交 type 选用说明
chore: 更新 VitePress 开发规范 element-plus 版本
```

校验：`commit-msg` → `pnpm check:commit` → `scripts/core/verify-commit.ts`

### 禁止写入大模型 / Agent 署名（必守）

提交信息**不得**包含任何 AI / Agent 相关 trailer 或署名，例如：

- `Co-authored-by: Cursor <cursoragent@cursor.com>`
- `Co-authored-by: Claude`、`Co-authored-by: GPT`、`Made-with: Cursor` 等同类行
- 正文中的「Generated by …」「via Cursor Agent」等机器生成声明

Agent 代为 `git commit` 时须使用干净的 Conventional Commits 正文；若环境自动注入上述 trailer，提交后须立刻用 `git commit --amend`（仅限未推送、且为本会话创建的 HEAD）去掉后再交付。

## 分支

| 变更 | 目标分支 |
|------|----------|
| 新特性 | `next` |
| bugfix / docs / 其他 | `main` |

npm 发布分支：`next`（`.npmrc`）

## 发布 Commit

- GitHub Release：`chore(release): ...`
- npm：`release(@142vip/xxx): ...`

## PR 自查（`.github/PULL_REQUEST_TEMPLATE/pr_cn.md`）

- 需求背景、方案、是否 breaking
- 文档 / 演示 / TS / Changelog 勾选
- 新特性 → `next`，需维护者审核

## Hooks

- `pre-commit` → `pnpm lint:fix`
- `commit-msg` → 格式校验

## Agent 约束

- 用户未要求：不 commit / push / 建 PR
- hook 失败：修复后**新 commit**，不 amend 已推送
- 禁止：`git config` 修改、`--no-verify`、main force push（除非明确要求）
- **禁止**在 commit message 中写入 `Co-authored-by: Cursor` 等大模型 / Agent 署名（见上文「禁止写入大模型 / Agent 署名」）
