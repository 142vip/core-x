---
description: 【必读】仓库架构与流程（与根 README Mermaid 同构）
alwaysApply: true
---

# 仓库架构与流程

> **对外图源**：根 `README.md`。本文件与根 README **五段 Mermaid 保持同构**（`10-Mermaid图表规范`）。**改图必两处同步**。

## 1. 仓库架构

```mermaid
flowchart TB
  ROOT["core-x · pnpm9 + turbo"]

  PKGS["packages/ · 30 × @142vip/*"]
  APPS["apps/ · 4 × *-demo"]
  VP[".vitepress/ · 根文档站 :8080"]
  DOCS["docs/ · index · apis · wiki"]
  SCR["scripts/ · ci · 发布 · commit"]
  GHA[".github/ · CI + CD"]

  ROOT --> PKGS
  ROOT --> APPS
  ROOT --> VP
  ROOT --> DOCS
  ROOT --> SCR
  ROOT --> GHA
  APPS --> PKGS
  VP --> PKGS
  DOCS --> VP
  SCR --> GHA
```

## 2. 技术分层

```mermaid
flowchart LR
  DEMO["apps/*-demo ×4"] --> PKG["@142vip/* ×30"]
  PKG --> UTIL["utils · open-source"]
  PKG --> CLI["fairy-cli · release-version · eslint-config"]
  PKG --> FW["Nest · Egg · VitePress · VuePress"]
```

## 3. 模块发布（ESM + CJS）

```mermaid
flowchart LR
  SRC["src/"] --> UB["unbuild · 17 包"]
  SRC --> TSC["tsc · Nest 5 包"]
  SRC --> EGG["Egg 插件 8 包 · 无 build"]
  UB --> DIST["dist .mjs + .cjs"]
  TSC --> DIST2["dist .js CJS"]
  EGG --> NPM["npm @142vip/*"]
  DIST --> EXP["exports"]
  DIST2 --> EXP
  EXP --> NPM
```

详见 `business/ESM与模块发布`。包清单见 `16-全仓资产清单`。

## 4. 开发与发布

```mermaid
sequenceDiagram
  participant Dev as 开发者
  participant Hook as git hooks
  participant CI as GitHub CI
  participant CD as GitHub CD
  participant NPM as npm

  Dev->>Dev: cd build · lint:fix · 文档同步
  Dev->>Hook: git commit
  Hook->>Hook: lint:fix · check:commit
  Dev->>CI: PR → next
  CI->>CI: scripts/ci · lint · build:docs
  CI->>CD: merge next
  CD->>NPM: release @142vip/*
  CD->>CD: GitHub Pages
```

## 5. 分支流

```mermaid
flowchart LR
  FEAT["feat/fix"] --> PR["PR"]
  PR -->|新特性| NEXT["next"]
  PR -->|其他| MAIN["main"]
  NEXT --> CD["CD"]
  CD --> NPM["npm"]
  CD --> PAGES["Pages"]
```

## 6. 本地开发流程

与根 README §本地开发 **逐字同构**。

```mermaid
flowchart TD
  A["./scripts/ci<br/>失败则 pnpm install"] --> B["改 packages / apps"]
  B --> C["cd 模块目录 && pnpm build"]
  C --> D["pnpm lint:fix · 0 error"]
  D --> E["同步文档 · README · sidebar"]
  E --> F{"预览?"}
  F -->|根站 :8080| G["pnpm dev"]
  F -->|demo :3080| H["cd apps/vitepress-demo && pnpm dev"]
  G --> I["pnpm build:docs"]
  H --> I
```

## 7. 模型工作流

见 `rules/README.md`（仅模型，不写入根 README）。

## 8. 核心约束

- packages：ESM 优先 + CJS 兼容（`business/ESM与模块发布`）
- Mermaid：根 README ↔ 本文件 **五段同构**；结构变化时同步（`10`）
- 任务结束：`lint:fix`（0 error）→ cd build → 文档自检（`09`）
