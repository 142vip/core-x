---
description: 【必读】启动、编译、打包、发布等常用命令
alwaysApply: true
---

# 常用命令手册

> 模型须按本手册执行命令。**安装优先 `./scripts/ci`**；**单包 build/test 优先进入模块目录**；全量与根级脚本在仓库根执行。

## 环境要求

- Node `>=18`
- pnpm `9.6.0`（`packageManager` 锁定，勿用 npm/yarn）

## 安装依赖（首选 scripts/ci，异常可回退）

```bash
# 首选：与 GitHub CI 一致（corepack + frozen-lockfile + 镜像源）
./scripts/ci

# 回退：./scripts/ci 失败或环境受限时
pnpm install
```

| 场景 | 命令 |
|------|------|
| 首次克隆 / 换机 / 与 CI 对齐 | `./scripts/ci` |
| **ci 报错**（网络、corepack、镜像等） | `pnpm install` |
| 日常增量、依赖已装全 | `pnpm install` |
| CI 定义 | `.github/workflows/CI.yml` → `./scripts/ci` |

**原则**：先试 `./scripts/ci`；有问题再 `pnpm install`，勿跳过安装直接 build。

详见 `15-scripts脚本手册` § scripts/ci。

## 单包命令（优先进入目录）

在对应 `packages/<dir>/` 或 `apps/<demo>/` 下执行 `pnpm <script>` 为**首选**；根目录 `pnpm --filter` 等价，仅在不便 `cd` 时使用。

| 场景 | 首选（模块目录） | 备选（仓库根） |
|------|------------------|----------------|
| 单包 build | `cd packages/vitepress && pnpm build` | `pnpm --filter @142vip/vitepress build` |
| 单包 test | `cd packages/utils && pnpm test` | `pnpm --filter @142vip/utils test` |
| 单包 typecheck | `cd packages/vitepress && pnpm typecheck` | `pnpm --filter @142vip/vitepress typecheck` |
| demo dev | `cd apps/vitepress-demo && pnpm dev` | `pnpm --filter vitepress-demo dev` |
| nest-demo | `cd apps/nest-demo && pnpm start` | `pnpm --filter nest-demo start` |
| egg-demo | `cd apps/egg-demo && pnpm dev` | `pnpm --filter egg-demo dev` |

**仅在仓库根执行**（无模块目录等价）：`pnpm dev` / `pnpm build:docs` / `pnpm build:packages` / `pnpm lint:fix` / `pnpm test`（turbo 全量）/ `pnpm build`。

选用原则：**改哪进哪** → `cd` 到目标目录执行；安装先试 `./scripts/ci`，失败再 `pnpm install`。

## CodeGraph（任务开始前必做）

```bash
codegraph sync    # 无索引 → codegraph init
codegraph status
codegraph query <关键词>
```

## 开发启动

| 场景 | 命令 | 端口 |
|------|------|------|
| 根文档站 | `pnpm dev`（根目录） | 8080 |
| 根文档站（代理） | `pnpm dev:proxy` | 8080 |
| vitepress-demo | `cd apps/vitepress-demo && pnpm dev` | 3080 |
| nest-demo | `cd apps/nest-demo && pnpm start` | 见 demo 配置 |
| egg-demo | `cd apps/egg-demo && pnpm dev` | 见 demo 配置 |

## 编译 / 打包

```bash
# 单包（首选进入目录）
cd packages/<pkg> && pnpm build
cd packages/<pkg> && pnpm typecheck    # 若有

# 全量（根目录）
pnpm build:packages                      # turbo @142vip/*
pnpm build:apps                          # turbo *-demo
pnpm build:docs                          # vitepress + typedoc:api
pnpm build                               # 完整构建
```

## 测试

```bash
cd packages/<pkg> && pnpm test
pnpm test                                # turbo 全量（根目录）
```

## 代码质量（提交前 / 任务结束）

```bash
pnpm lint                                # 0 error，warning 允许
pnpm lint:fix                            # 必须执行
```

## 构建验证（lint:fix 之后）

```bash
# 局部（首选进入目录）
cd packages/<pkg> && pnpm build
cd apps/<demo> && pnpm build

# 保底（根目录）
pnpm build
```

## 清理

```bash
pnpm clean:cache
pnpm clean:dist
pnpm clean                               # 慎用
```

## 发布与检查

```bash
pnpm check:commit
pnpm check:release
pnpm check:publish
pnpm release                             # 维护者
```

发布分支：`next`（`.npmrc` → `publish-branch=next`）

## TypeDoc

```bash
pnpm typedoc:api
pnpm typedoc:wiki
pnpm build:docs
```

## 模型选用原则

```
安装      →  ./scripts/ci；失败则 pnpm install
改单包    →  cd packages/<pkg> && pnpm build [test] [typecheck]
改 demo   →  cd apps/<demo> && pnpm dev/build
根级任务  →  pnpm lint:fix / build:docs / pnpm build（根目录）
任务结束  →  lint:fix（0 error）→ 牵连目录 build → 不明则 pnpm build
文档      →  与代码一致，30 包 + 4 demo 不遗漏（09）
```
