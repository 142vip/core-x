---
description: 代码、README、docs/ 与 VitePress 同步；禁止在仓库内引用 rules
alwaysApply: true
---

# 文档同步规范

**代码变 → 文档变**（同一任务内）。权威清单：`16-全仓资产清单`。

## 文档地图

| 位置 | 内容 | 维护方式 |
|------|------|----------|
| **根 `README.md`** | GitHub 入口：包列表、命令、**五段架构 Mermaid** | 手改；**与 `07` 逐段同构**（`10`） |
| **`docs/index.md`** | VitePress 首页 frontmatter（hero/features）；可选 Markdown 正文 | **HomePage** 由 `.vitepress/theme` 插槽注入，勿在 md 中 import 组件 |
| **`docs/tech.md`** | 维护者：npm/docker 登录 | 手改；与脚本/CI 一致 |
| **`docs/media.md`** | 自媒体 | 手改 |
| **`docs/apis/`** | API 参考 | `pnpm typedoc:api` / `build:docs` 生成，**勿手改** |
| **`docs/wiki/`** | Wiki | `pnpm typedoc:wiki` 生成，**勿手改** |
| **`packages/*/README.md`** | 模块文档 → `/packages/:pkg/` | 改 API 时手改 |
| **`apps/*/README.md`** | demo 文档 → `/apps/:pkg/` | 改 demo 时手改 |
| **`.vitepress/`** | 导航、侧栏、首页主题 | 见下表 |

### `.vitepress/` 与代码

| 文件 | 同步点 |
|------|--------|
| `config.ts` | nav、rewrites、版本号 |
| `sidebar.ts` | **31 包 + 4 demo**；npm 名准确（如 `oauth2.0`） |
| `theme/components/project-data.ts` | `getTableData('example' \| 'project')`：侧栏 + `package.json` |
| `theme/components/HomePage.vue` | 首页展示（经 `defineVipExtendsTheme` 的 `homePage`，挂在正文下方） |
| `theme/index.ts` | `defineVipExtendsTheme` + `homePage` |
| `docs/index.md` | hero/features frontmatter；链接指向现存包 |

## 完整性（禁止丢模块）

- 根 README 包列表：**31** 个 `@142vip/*`
- `sidebar.ts`：**31 包 + 4 demo**，无死链、无幽灵条目
- 仅当仓库**真实删除**目录后，才可从上述位置移除

## 变更 → 同步

| 代码变更 | 文档动作 |
|----------|----------|
| 增删包/demo | README 列表、`sidebar.ts`、`16`、对应 README |
| 公开 API | 包 README + `typedoc:api` 或 `build:docs` |
| 命令/端口 | README「使用」、`08`、包/demo README |
| 架构/数量/CI/本地流程 | **根 README 五段 Mermaid** + `07` 同构（`10` 同步铁律） |
| VitePress 主题 | `packages/vitepress` + `.vitepress/` + `vitepress-demo` |

## 交付前自检（必做）

```
□ README 31 包 + 4 demo 完整？
□ sidebar 与 packages/、apps/ 一一对应？oauth 显示 oauth2.0？
□ docs/index.md 仅为首页（无重复 README 架构段）？
□ 改动包的 README、docs/tech 等手改文档已更新？
□ API 变更已 build:docs？
□ README 与 07 **五段 Mermaid 逐段同构**（图1~5，见 `10` 清单）？
□ rules 外无 .cursor/rules 引用？
```

Mermaid 细则：`10`。Markdown 细则：`17`。
