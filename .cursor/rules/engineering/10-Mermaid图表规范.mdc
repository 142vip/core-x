---
description: Mermaid 图表编写规范（与根 README 图表保持一致）
alwaysApply: true
---

# Mermaid 图表规范

仓库架构/流程**一律 Mermaid**。对外唯一图源：**根 `README.md`**；模型内镜像：**`07-仓库架构与流程`**。

## 同步铁律（每次变更必守）

```
改什么              →  必须更新哪张图
─────────────────────────────────────────
包/demo 增删         →  图1 仓库架构（数量）· 图2 技术分层（若分类变）
构建方式变化         →  图3 模块发布
CI/CD/钩子/分支变    →  图4 开发与发布 · 07§5 分支流
本地命令/流程变      →  图5 本地开发 · README「使用」
任何上述变化         →  根 README 先改 → 07 同构复制 → 09 自检勾选
```

**禁止**：只改代码不改图；只改 `07` 不改 README；在 `docs/index.md` 重复粘贴五段图（`09`）。

## 五段图表清单（与仓库当前一致）

### 图1 · 仓库架构 `flowchart TB`

节点：`ROOT` → `PKGS`(30) / `APPS`(4) / `VP`(.vitepress:8080) / `DOCS`(docs/) / `SCR`(scripts/) / `GHA`；`DOCS→VP`，`SCR→GHA`。

### 图2 · 技术分层 `flowchart LR`

`demo×4` → `pkg×30` → `utils·open-source` / `CLI三件套` / `四大框架族`。

### 图3 · 模块发布 `flowchart LR`

`unbuild·17` / `tsc·Nest5` / `Egg插件8无build` → `exports` / 直发 → `npm`。

### 图4 · 开发与发布 `sequenceDiagram`

本地：`cd build · lint:fix · 文档同步` → commit → hooks → PR next → CI(`scripts/ci·lint·build:docs`) → CD → npm + Pages。

### 图5 · 本地开发 `flowchart TD`

`scripts/ci（失败pnpm install）` → 改代码 → `cd build` → `lint:fix` → 同步文档 → 预览 dev → `build:docs`。

`07` 另含 **§5 分支流**（仅 rules，不在根 README）。

## 变更时操作步骤

1. 根据上表判断受影响图表编号
2. **先改**根 `README.md` 对应 ` ```mermaid ` 块
3. **复制同构**到 `07` 对应章节（§1~§6）
4. 若涉及包数量：同步 README 包列表、`sidebar.ts`、`16`
5. `09` 交付自检勾选「README 与 07 五段 Mermaid 同构」

## 画法原则

1. **少用 subgraph**；节点 + 箭头直连
2. **数字准确**：31 包 · 4 demo · unbuild 18 · Nest 5 · Egg 8
3. **端口**：根站 8080 · vitepress-demo 3080
4. 单图节点 ≤ 15

## 语法

- 围栏独立成行 ` ```mermaid `，前后空行
- 节点 ID：英文数字下划线；显示文本放 `["..."]`
- 换行用 `<br/>`；含 `:` 的文本用双引号

## 禁止

- README 与 `07` 五段不一致
- 对外文档引用 `.cursor/rules`（`14`）
- 截图代替 Mermaid
