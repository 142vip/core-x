---
description: 模型有义务将新知识沉淀到 rules，保持规则与代码同步
alwaysApply: true
---

# 规则维护与知识沉淀

## rules 的定位

`.cursor/rules/` 是**模型的内部知识库**：把本次对话中读代码、跑命令、踩坑得到的**已验证事实**写下来，供下次任务直接复用。不是对外文档，不替代根 README / 包 README（`14`）。

## 模型义务

任务中产生且**可复用**的知识，模型有义务更新到 `.cursor/rules/`，避免下次重复踩坑：

| 发现 | 更新目标 |
|------|----------|
| 新命令 / 端口 / 安装方式 | `08`、**根 README**「使用」 |
| scripts 行为变化 | `15-scripts脚本手册` |
| 新目录 / 新包 | `16-全仓资产清单`、`04`、`business/包职责地图` |
| 架构 / ESM | **根 README** + `07` 五段 Mermaid 同构（`10`） |
| Markdown 约定 | `17` |
| VitePress / `.vitepress` | `09` 自检表、`business/VitePress开发`、根 README |
| exports / 构建 | `business/ESM与模块发布`、包 README |
| 模块写法 | `business/` 对应文件 |
| 流程 / 验收 | `01`、`02` |
| 待办 | `TODO.md` |
| 规则边界 | `14` |

**用户可见内容**写入根 README 或包 README，不在仓库其他处引用 rules（`14`）。

## 更新原则

- **小步更新**：只改与本次任务相关的规则段落
- **不臆造**：只写入已验证的事实（跑通过的命令、读过的代码）
- **去重**：新知识与现有规则重复则合并，不堆叠
- **同步多份**：架构变化时，根 README 与 `07` 的 Mermaid 须一致

## 何时不必更新 rules

- 一次性调试结论、未验证的猜测
- 仅影响当前 PR 的临时方案
- 用户明确要求不要改 rules

## 交付时说明

若本次更新了 rules，交付中简要列出：

```
规则更新：engineering/08（新增 xxx 命令）· business/VitePress开发（xxx 约定）
```

## 自检

```
□ 本次是否有应沉淀但未写入 rules 的约定？
□ rules 中 Mermaid / 命令 / 路径是否与代码一致？
□ 是否在 rules 外新增 `.cursor/rules` 引用（`14`）？
```
