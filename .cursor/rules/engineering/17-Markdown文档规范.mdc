---
description: Markdown 文档语法规范（README、docs、rules 内 .md）
alwaysApply: true
---

# Markdown 文档规范

仓库内所有 `.md` 文档（根 README、包 README、`docs/`、`.cursor/rules/*.md`）须符合标准 Markdown，保证 VitePress / GitHub / 编辑器一致渲染。

## 1. 标题

- 使用 ATX 标题 `#` … `######`，**`#` 后必须有空格**
- 标题前后各留**一行空行**
- 每文件仅一个一级标题（`README` 通常为包名或文档名）
- 不跳级（`##` 后直接 `####` 禁止）

## 2. 代码块

- 围栏单独成行：`` ```lang ``，闭合 `` ``` ``
- 围栏前后各留空行
- 标明语言：`shell`、`ts`、`mermaid`、`json` 等
- Mermaid 块遵循 `10-Mermaid图表规范`

## 3. 列表

- 无序列表统一用 `-`，同级缩进 2 空格
- 有序列表数字后加点和空格：`1. `
- 列表块前后留空行；列表项内多段落须缩进

## 4. 链接与图片

- 链接：`[描述](url)`，描述有意义，不用「点击这里」
- 站内路径与 VitePress rewrite 一致（如 `/packages/fairy-cli/`）
- 图片：`![alt](url)`，**alt 必填**

## 5. 表格

- 表头行与分隔行 `|---|---|` 对齐
- 单元格内避免未转义的 `|` 与裸 HTML
- 列数每行一致

## 6. 强调与行内代码

- 行内代码用单个反引号 `` ` ``，勿用反引号嵌套未转义
- 粗体/斜体不跨行；技术标识符优先用行内代码而非粗体

## 7. 禁止

- 标题行尾留空格、Tab 缩进混用
- HTML 标签代替 Markdown（除非 VitePress  frontmatter / 首页特例）
- 未闭合围栏、错误嵌套引用块
- 在 **TypeDoc 生成** 的 `docs/apis/`、`docs/wiki/` 中手改（应改源码 + 重新生成）

## 8. 文档类型分工

| 类型 | 规范 |
|------|------|
| 根 `README.md` | 用户入口；架构图与 `07` 同构；见 `09` |
| 包 `README.md` | API、安装、示例与 `exports` 一致 |
| `docs/index.md` | VitePress 首页 frontmatter + 组件 |
| `.cursor/rules/*.md` | 给人看的索引；**不**出现在对外文档 |

## 9. 自检

```
□ 标题层级连续、空行正确？
□ 代码块语言与闭合正确？
□ 链接可访问、包名与 npm 一致（如 oauth2.0）？
□ 修改 API 文档是否通过 typedoc 生成而非手改 apis/？
```
