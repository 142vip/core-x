# <ProjectName> 项目开发指南

> **L0 真源**：本仓库 AI / Agent 规范以本文件为准。
> 通用流程 skill 来自已安装的 `@142vip/agent-skills`（`code-dev` / `self-check` / `commit`）。
> 用 `pnpm exec vip-agent-skills --target .` 刷到 `.agents/skills/`；`--check` 可检测漂移。
> **业务落点 / 模块地图**（若有）只放在本仓 `.agents/skills/` 下的本地 skill，**永不**打进 `@142vip/agent-skills`。

---

## 项目信息

- **类型**：（monorepo / 单仓 · 包管理器 · Node 版本）
- **应用 / 包边界**：（列表）
- **禁止**：……

## 通用编码规范

（类型、async、注释、目录导出……可引用 core skill，此处写**本仓强制项**）

## 栈约定（覆盖 core 占位）

| 场景 | 本仓要求 |
|------|----------|
| 日期 / 时间 | |
| HTTP 状态码 | |
| HTTP 客户端 | |
| 更新类 REST | |

## 验证与构建

详见 `.agents/project/build-map.md`（推荐）或下文命令表。

## Git 与提交

- Conventional Commits
- **scope 枚举**：（填写本仓应用/包名）
- Agent：只 commit，不擅自 push

## AI / Agent

```bash
pnpm exec vip-agent-skills --target .         # 同步通用 skills 到 .agents/skills
pnpm exec vip-agent-skills --target . --check # 比对是否与已安装包一致
```

可选：根目录 `TODO.md`（仅未完成项）。`self-check` 在任务结束后会清理已完成项，并按优先级推荐后续可执行待办。

冲突优先级：`AGENTS.md`（本文件）> `.agents/project/*` > 已同步的 core skills。
